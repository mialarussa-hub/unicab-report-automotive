"""Report assembly service — combines data, sentiment, ADV, and AI drafts into a report."""


async def assemble_draft(reference_month: int) -> dict:
    """Assemble a draft report for a given month.

    Orchestrates: data retrieval → sentiment scores → ADV data → AI editorial → draft.
    """
    # TODO: implement in assemblaggio sprint (settimane 4-5)
    raise NotImplementedError("Report assembly — to be implemented")
